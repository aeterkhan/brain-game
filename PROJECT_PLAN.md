# brain_game 확장 기획안

> 작성일: 2026-06-30 · 기준 버전: `brain-study-game_v0.3.html` (배포: https://aeterkhan.github.io/brain-game/)

---

## 1. 현재 상태 진단

### 1.1 "무겁지 않은가?" — 결론: 지금은 전혀 안 무겁다

| 항목 | 수치 | 평가 |
|---|---|---|
| HTML 원본 | 54KB | 가벼움 |
| gzip 전송 크기 | **17KB** | 매우 가벼움 (이미지 1장보다 작음) |
| JS 런타임 | 바닐라, 외부 라이브러리 0개 | 프레임워크 오버헤드 없음 |
| 외부 의존성 | Google Fonts 4패밀리(Gowun Batang/Dodum/Gaegu/Space Mono) | **실질적 부하의 대부분** |

**핵심:** 페이지 자체(17KB)는 무게 걱정이 무의미합니다. 체감 로딩의 80%는 폰트 4종이 좌우합니다. 지금 단계에서 최적화한다면 폰트부터:
- 필요 굵기만 로드 (현재 Gowun Batang 400/700, Gaegu 400/700 모두 호출)
- `font-display:swap` 확인 (현재 적용됨)
- 향후 폰트 self-host + `unicode-range` subset(한글 서브셋)으로 전환하면 폰트 전송량 절반 이하로 감소

### 1.2 단일 HTML의 진짜 문제는 "무게"가 아니라 "확장성"

단일 파일은 MVP로는 정답입니다. 문제는 콘텐츠/기능이 늘어날 때 드러납니다:

- **콘텐츠와 코드가 한 파일에 섞임** — 퀴즈 문항(`QUIZ2_POOL` 등)이 JS 안에 하드코딩. 문항 추가/수정 시 코드를 건드려야 하고, 비개발자(콘텐츠 기획자)가 손댈 수 없음.
- **주제 확장이 곧 파일 비대화** — 지금은 "공부법" 1개 주제. "기억의 궁전", "포모도로 트레이너", "집중력 게임" 등을 추가하면 한 파일이 수천 줄로 부풀고 유지보수 불가.
- **상태 비영속** — 새로고침하면 진도·점수가 사라짐. 간격반복 "예약"이 실제 알림으로 이어지지 않음.
- **재사용 불가** — 퀴즈 렌더러, 드래그, 타이머가 전역 함수로 묶여 다른 게임에서 재활용 어려움.
- **테스트 부재** — 자동화 테스트가 없어 회귀 검증을 수동 브라우저 확인에 의존.

---

## 2. 확장 비전

`brain_game`을 **"뇌과학 원리를 직접 손으로 익히는 인터랙티브 학습 모음"**으로 키운다.
현재 "백지의 방"(공부법)은 첫 번째 "방". 같은 디자인 언어와 게임 엔진 위에 여러 방을 얹는다.

```
brain_game (플랫폼)
├── 백지의 방      — 인출연습 5회독 (현재 v0.3)
├── 기억의 궁전     — 장소법 암기 훈련
├── 포모도로 도장   — 집중 루틴 + 통계
├── 도파민 리셋     — 숏폼 디톡스 챌린지 (원 게시물의 "프로필 링크" 콘텐츠)
└── ...            — 방 추가 = 데이터 + 씬(scene) 추가
```

---

## 3. 아키텍처 진화 단계

### Phase 0 — 현재 (단일 HTML MVP) ✅
지금. 검증 끝. 그대로 데모로 운영.

### Phase 1 — 구조 분리 (라이브러리 없이도 가능, 1~2일)
**목표: 콘텐츠를 코드에서 떼어낸다.** 프레임워크 없이도 즉시 효과.

- 퀴즈/단계 데이터를 별도 `content/study-room.json`으로 분리 → 코드 수정 없이 문항 추가
- CSS/JS를 `style.css`, `app.js`로 외부 파일화 (캐시 가능, 가독성 향상)
- ES Module로 기능 분리: `quiz.js`, `flashcards.js`, `retrieval.js`, `reorder.js`, `timer.js`
- 폰트 최적화(서브셋/필요 굵기만)

### Phase 2 — 빌드 도구 + 멀티 게임 (1주)
**목표: 방을 여러 개 담는 플랫폼으로.**

- **Vite** 도입 (제로 설정에 가깝고, 정적 빌드 결과는 여전히 GitHub Pages 배포 가능)
- 라우팅: 가벼운 해시 라우터 또는 작은 SPA. 홈(방 선택) → 각 방
- "게임 엔진" 추상화: 모든 방이 공유하는 `Stage`/`Scene` 인터페이스, 진행 트래커, 점수 시스템
- 프레임워크 선택지(아래 4장 참고): **바닐라 유지** vs **경량 프레임워크(Svelte/Preact)**

### Phase 3 — 상태 영속 + 학습 사이클 (1~2주)
**목표: "한 번 하고 끝"을 "꾸준히 쓰는 도구"로.**

- `localStorage`로 진도/점수/마지막 학습일 저장 (백엔드 없이 시작)
- **간격반복 실제 구현**: 학습 항목별 다음 복습일 계산(SM-2 유사 알고리즘), 홈에서 "오늘 복습할 것" 표시
- 복습 알림: `.ics` 캘린더 다운로드 → 추후 웹 푸시
- PWA 전환: 오프라인 사용, 홈 화면 설치

### Phase 4 — 백엔드 + 계정 (선택, 수요 검증 후)
**목표: 기기 간 동기화, 통계, 콘텐츠 운영.**

- 백엔드: Supabase 또는 Cloudflare(D1/Workers) — 서버리스로 운영 부담 최소화
- 사용자 계정, 학습 기록 클라우드 동기화
- 학습 통계 대시보드, 콘텐츠 CMS(비개발자가 방/문항 추가)
- A/B 테스트, 익명 분석

---

## 4. 기술 스택 추천

| 영역 | 추천 | 이유 |
|---|---|---|
| 빌드 | **Vite** | 빠르고, 정적 출력이 GitHub Pages와 호환 |
| 프레임워크 | **Svelte** (또는 바닐라 유지) | 런타임 작음(현재 게임의 가벼움 유지), 컴포넌트로 방 재사용 쉬움. React는 이 규모엔 과함 |
| 언어 | TypeScript | 콘텐츠 데이터 스키마 타입 보장 (문항 구조 실수 방지) |
| 상태 | localStorage → (Phase 4) Supabase | 점진적. 백엔드는 필요할 때만 |
| 배포 | GitHub Pages (현재) → 필요 시 Cloudflare Pages | 둘 다 무료, 정적 |
| 테스트 | Vitest + Playwright | 로직 단위 테스트 + 핵심 플로우 E2E |

> **보수적 대안:** 외부 의존성 0개라는 현재 장점을 유지하고 싶다면, Phase 2까지 **바닐라 + ES Module + Vite**만으로도 충분합니다. 프레임워크는 방이 5개 이상으로 늘거나 컴포넌트 재사용 비용이 커질 때 도입.

---

## 5. 제안 디렉터리 구조 (Phase 2 기준)

```
brain_game/
├── index.html              # 홈(방 선택)
├── src/
│   ├── main.ts             # 진입점 + 라우터
│   ├── engine/             # 공유 게임 엔진
│   │   ├── Stage.ts        #   단계/씬 추상화
│   │   ├── progress.ts     #   진행 트래커
│   │   ├── score.ts        #   점수 시스템
│   │   └── spaced.ts       #   간격반복 알고리즘
│   ├── components/         # 재사용 UI (퀴즈, 플래시카드, 드래그, 타이머)
│   ├── rooms/              # 각 게임("방")
│   │   ├── study-room/     #   백지의 방 (현재 v0.3)
│   │   ├── memory-palace/
│   │   └── pomodoro/
│   └── styles/             # 디자인 토큰(현재 :root 변수) + 공통 CSS
├── content/                # 콘텐츠 데이터 (코드와 분리)
│   ├── study-room.json     #   퀴즈/단계 데이터
│   └── ...
├── public/fonts/           # self-host 서브셋 폰트
├── tests/                  # Vitest + Playwright
└── PROJECT_PLAN.md
```

---

## 6. 데이터 모델 예시 (콘텐츠 분리)

현재 JS에 하드코딩된 문항을 이렇게 외부화:

```json
{
  "room": "study-room",
  "title": "백지의 방",
  "stages": [
    { "type": "flashcards", "data": "steps" },
    { "type": "quiz", "pool": "quiz2", "concepts": ["절약형 뇌", "입력vs인출", "유창성 착각"] },
    { "type": "retrieval", "keys": "retrieveKeys" },
    { "type": "quiz", "pool": "quiz4" },
    { "type": "reorder", "data": "steps" }
  ]
}
```

이렇게 하면 새 방 = 새 JSON + (필요 시) 새 스테이지 타입만 추가. 엔진은 그대로.

---

## 7. 로드맵 (우선순위)

| 순위 | 작업 | 기대 효과 | 규모 |
|---|---|---|---|
| 1 | 폰트 최적화 (서브셋/필요 굵기) | 체감 로딩 즉시 개선 | 0.5일 |
| 2 | 콘텐츠 JSON 분리 (Phase 1) | 문항 추가가 코드 수정 없이 가능 | 1일 |
| 3 | localStorage 진도 저장 | 새로고침해도 이어하기 | 0.5일 |
| 4 | Vite + 모듈 분리 (Phase 2) | 멀티 게임 기반 | 2~3일 |
| 5 | 간격반복 실제 구현 (Phase 3) | "꾸준히 쓰는 도구"로 전환 | 3일 |
| 6 | 두 번째 방 추가 | 플랫폼 검증 | 3일 |

---

## 8. 권장 의사결정

1. **지금 당장은 성능 최적화 불필요.** 17KB는 충분히 빠르다. 폰트만 손보면 끝.
2. **다음 한 걸음은 "콘텐츠 분리"(Phase 1).** 가장 적은 노력으로 가장 큰 유지보수 이득. 프레임워크 도입보다 우선.
3. **프레임워크는 방이 2~3개로 늘 때 도입.** 그 전엔 바닐라+Vite로 충분.
4. **백엔드는 사용자 수요가 검증된 뒤.** 그때까지 localStorage로 버틴다.
